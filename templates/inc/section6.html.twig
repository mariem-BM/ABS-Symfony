{#{% if articles %}
	<div class="owl-carousel owl-carousel1 owl-theme">
		<div class="hero">
			<div class="hero__container">
				<div class="hero__container--left">
					<h2>INSIGHTS</h2>
				</div>
				<div class="hero__container--right">
					<button class="hero__container--btn">
						<a href="#">See all insights</a>
					</button>
				</div>
			</div>
			<div class="container-fluid">
				<div class="row">
					{% for article in articles %}
						<div class="col-md-4" {% if loop.index0 == 0 %} class="item active" {% else %} class="item" {% endif %}>
							<img class="img-responsive center-block" src="{{asset('uploads/brochures/'~article.picture)}}" height="300" width="400" alt=""/>


							<p>{{ article.content }}</p>
							<a {{ article.readmore}}>Read more...</a>
							<p>{{ article.publicationDate }}</p>
							<p>{{ article.lastUpdateDate }}</p>


						</div>

					{% endfor %}


				</div>
			</div>

		</div>
	</div>
{% endif %}#}
{% if articles %}
<div class="hero">
		<div class="hero__container">
				<div class="hero__container--left">
					<h2>INSIGHTS</h2>
				</div>
				<div class="hero__container--right">
					<button class="hero__container--btn">
						<a href="#">See all insights</a>
					</button>
				</div>
			</div>
		<div class="container-fluid">
			<div id="carouselExample" class="carousel slide" data-ride="carousel" data-interval="2250">
        <div class="carousel-inner row w-10 mxauto" role="listbox">
           
					
						{% for article in articles %}
						   
							
							 <div class="carousel-item col-md-4 {% if loop.index0 == 0 %} active {% endif %}">
							
									<img class="img-responsive center-block" src="{{asset('uploads/brochures/'~article.picture)}}" height="300" width="400" alt=""/>

<br>
							<p>{{ article.content }}</p>
							<a {{ article.readmore}}>Read more...</a>
							<p>{{ article.publicationDate }}</p>
							<p>{{ article.lastUpdateDate }}</p>

								</div>
							
						{% endfor %}
 
					</div>
					   <a class="carousel-control-prev" href="#carouselExample" role="button" data-slide="prev">
            <i class="fa fa-chevron-left fa-lg text-muted"></i>
            <span class="sr-only">Previous</span>
        </a>
                                        <a class="carousel-control-next text-faded" href="#carouselExample" role="button" data-slide="next">
            <i class="fa fa-chevron-right fa-lg text-muted"></i>
            <span class="sr-only">Next</span>
        </a>
					

				</div>
		</div>	
	</div>
{% endif %}

