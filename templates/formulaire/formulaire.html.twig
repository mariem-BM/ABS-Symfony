{% extends 'back.html.twig' %}

{% block title%}
	Liste des formulaires
{% endblock %}
{% block content %}
	{% if formulaires %}
		<table id="formulaires" class="table table-striped">
			<thead>
				<tr>
					<th>name</th>
					<th>lastname</th>
					<th>email</th>
					<th>message</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for formulaire in formulaires %}
					<tr>
						<td>{{ formulaire.name }}</td>
						<td>{{ formulaire.lastname }}</td>
						<td>{{ formulaire.email }}</td>
						<td>{{ formulaire.message }}</td>
						<td>

							<a href="/formulaire/edit/{{ formulaire.id }}" class="btn btn-dark">Modifier</a>
							<a href="/formulaire/delete/{{ formulaire.id }}" class="btn btn-danger" onclick="return confirm('Etes-
																																																																																																																																																vous sûr de supprimer cet article?');">Delete</a>

						</td>
					</tr>
				</td>
			</tbody>
		</tr>

	{% endfor %}
</tbody></table>{% else %}<p>Aucun Service</p>{% endif %}{% endblock %}
